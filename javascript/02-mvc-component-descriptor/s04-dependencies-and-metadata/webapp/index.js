sap.ui.define([], () => {
	"use strict";

	// From this stage on, the component is started declaratively from index.html
	// (the data-sap-ui-component element), so this file is no longer needed. It
	// is left empty on purpose; you could safely delete it.
});
